import React from "react";
import ReactDOM from "react-dom/client";
import JessySpeechApp from "./jessy-speech-coach.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <JessySpeechApp />
  </React.StrictMode>
);
